on:
  workflow_run:
    workflows: ["push", "tag"]
    branches: [next]
    types: 
      - completed
      
jobs:
  check-requirements:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "on.workflow_run"
